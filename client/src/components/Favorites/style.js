export default (theme) => {
    return {
        root: {
            backgroundColor: theme.background.dark,
            paddingTop: "10px",
            minHeight: 36
        },
        quote: {
            color: "grey"
        }
    }
};
